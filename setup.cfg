[metadata]
name = safeagent-exec-guard
description = AI agent execution guard with durable claims, request-id deduplication, unresolved-outcome handling, and receipts.
long_description = file: README.md
long_description_content_type = text/markdown
keywords = ai agents, idempotency, duplicate suppression, reliability, execution guard, deduplication, action-ref, jcs, mycelium, soma, webhook deduplication, on-chain audit
